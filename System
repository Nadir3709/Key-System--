-- Nadir Hub
local ScreenGui = Instance.new("ScreenGui")
local Main = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local NadirHub = Instance.new("TextLabel")
local UICorner_2 = Instance.new("UICorner")
local Enter = Instance.new("TextButton")
local UICorner_3 = Instance.new("UICorner")
local JoinDiscord = Instance.new("TextButton")
local UICorner_4 = Instance.new("UICorner")
local Key = Instance.new("TextBox")
local UICorner_5 = Instance.new("UICorner")
local Close = Instance.new("TextButton")
local UICorner_6 = Instance.new("UICorner")

--Properties:

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Main.Name = "Main"
Main.Parent = ScreenGui
Main.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Main.Position = UDim2.new(0.313986689, 0, 0.314960629, 0)
Main.Size = UDim2.new(0, 391, 0, 235)

UICorner.Parent = Main

NadirHub.Name = "Nadir Hub"
NadirHub.Parent = Main
NadirHub.BackgroundColor3 = Color3.fromRGB(36, 36, 36)
NadirHub.Size = UDim2.new(0, 391, 0, 41)
NadirHub.Font = Enum.Font.SourceSans
NadirHub.Text = "Nadir Hub | Key System"
NadirHub.TextColor3 = Color3.fromRGB(255, 255, 255)
NadirHub.TextScaled = true
NadirHub.TextSize = 14.000
NadirHub.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
NadirHub.TextWrapped = true

UICorner_2.Parent = NadirHub

Enter.Name = "Enter"
Enter.Parent = Main
Enter.BackgroundColor3 = Color3.fromRGB(36, 36, 36)
Enter.Position = UDim2.new(0.0179028139, 0, 0.748936176, 0)
Enter.Size = UDim2.new(0, 166, 0, 50)
Enter.Font = Enum.Font.SourceSans
Enter.Text = "Enter"
Enter.TextColor3 = Color3.fromRGB(255, 255, 255)
Enter.TextScaled = true
Enter.TextSize = 14.000
Enter.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
Enter.TextWrapped = true
Enter.MouseButton1Down:connect(function()
	if Key.Text == "ya...idk" then
		Main.Visible = false

		loadstring(game:HttpGet("https://raw.githubusercontent.com/Nadir3709/Loading/main/Loading"))()
	end
end)

UICorner_3.Parent = Enter

JoinDiscord.Name = "Join Discord"
JoinDiscord.Parent = Main
JoinDiscord.BackgroundColor3 = Color3.fromRGB(36, 36, 36)
JoinDiscord.Position = UDim2.new(0.554987192, 0, 0.748936176, 0)
JoinDiscord.Size = UDim2.new(0, 166, 0, 50)
JoinDiscord.Font = Enum.Font.SourceSans
JoinDiscord.Text = "Copy Key Link (Click Me)"
JoinDiscord.TextColor3 = Color3.fromRGB(255, 255, 255)
JoinDiscord.TextScaled = true
JoinDiscord.TextSize = 14.000
JoinDiscord.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
JoinDiscord.TextWrapped = true
JoinDiscord.MouseButton1Down:connect(function()
	setclipboard("https://pastebin.com/raw/0Fe1Fa1W")

	local CoreGui = game:GetService("StarterGui")

	CoreGui:SetCore("SendNotification", {
		Title = "Key";
		Text = "Link Copy To Clipboard";
		Duration = 15;
	})
end)

UICorner_4.Parent = JoinDiscord

Key.Name = "Key"
Key.Parent = Main
Key.BackgroundColor3 = Color3.fromRGB(36, 36, 36)
Key.BorderColor3 = Color3.fromRGB(36, 36, 36)
Key.Position = UDim2.new(0.0179028139, 0, 0.361702114, 0)
Key.Size = UDim2.new(0, 376, 0, 50)
Key.Font = Enum.Font.SourceSans
Key.Text = "Key Here"
Key.TextColor3 = Color3.fromRGB(255, 255, 255)
Key.TextScaled = true
Key.TextSize = 14.000
Key.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
Key.TextWrapped = true

UICorner_5.Parent = Key

Close.Name = "Close"
Close.Parent = Main
Close.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Close.BorderColor3 = Color3.fromRGB(0, 0, 0)
Close.Position = UDim2.new(0.94373399, 0, 0, 0)
Close.Size = UDim2.new(0, 22, 0, 22)
Close.Font = Enum.Font.SourceSans
Close.Text = "X"
Close.TextColor3 = Color3.fromRGB(255, 0, 0)
Close.TextScaled = true
Close.TextSize = 14.000
Close.TextStrokeColor3 = Color3.fromRGB(255, 0, 0)
Close.TextWrapped = true

UICorner_6.Parent = Close

-- Scripts:

local function AVNYQF_fake_script() -- Main.Drag 
	local script = Instance.new('LocalScript', Main)

	local UIS = game:GetService('UserInputService')
	local frame = script.Parent
	local dragToggle = nil
	local dragSpeed = 0.25
	local dragStart = nil
	local startPos = nil
	
	local function updateInput(input)
		local delta = input.Position - dragStart
		local position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
			startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		game:GetService('TweenService'):Create(frame, TweenInfo.new(dragSpeed), {Position = position}):Play()
	end
	
	frame.InputBegan:Connect(function(input)
		if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then 
			dragToggle = true
			dragStart = input.Position
			startPos = frame.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragToggle = false
				end
			end)
		end
	end)
	
	UIS.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			if dragToggle then
				updateInput(input)
			end
		end
	end)
end
coroutine.wrap(AVNYQF_fake_script)()
local function TVRN_fake_script() -- Close.Close Script 
	local script = Instance.new('LocalScript', Close)

	local Frame = script.Parent.Parent
	
	script.Parent.MouseButton1Click:Connect(function()
		Frame.Visible = false
	end)
end
coroutine.wrap(TVRN_fake_script)()
